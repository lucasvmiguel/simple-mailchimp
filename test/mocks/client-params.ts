import { IClient } from '../../src/index'

const client: IClient = {
  apiKey: 'test-api-key',
  url: 'http://sample.com',
  username: 'test-username',
  listId: 'test-list-id',
  logging: false,
}

export default client;